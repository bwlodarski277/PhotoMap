package bwlodarski.courseworkapplication.Static;

/**
 * Shared Preferences names class
 * Used for referencing shared preferences in files that need the names.
 */
public final class UserPrefs {
	/**
	 * Shared preferences file name where user data is stored.
	 */
	public static final String userPrefFile = "USER";
	/**
	 * User ID key used to store the user ID in shared preferences.
	 */
	public static final String userIdKey = "ID_KEY";
	/**
	 * Username key used to store the username in shared preferences.
	 */
	public static final String usernameKey = "NAME_KEY";
}
