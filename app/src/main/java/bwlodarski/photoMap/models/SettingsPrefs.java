package bwlodarski.photoMap.models;

/**
 * Preferences for app settings
 * Used for referencing preferences in files that need the names.
 */
public final class SettingsPrefs {
	/**
	 * Shared preferences file name where user data is stored.
	 */
	public static final String settingsPrefFile = "SETTINGS";
	/**
	 * Key used when referencing how many columns to use in the grid.
	 */
	public static final String colsKey = "COLS";
}
